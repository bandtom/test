默认系统只有一个C盘分区，但自己又想分多个(D/E/F/G)盘分区。

以下教程使用系统自带工具实现分区！

Win+R键打开运行 输入 compmgmt.msc 回车，

打开计算机管理-磁盘管理-选中要分出其他分区的磁盘，右键 压缩卷，输入要压缩的空间量，这里就是你想要分出多少储存空间的意思。按提示操作下来。

之后就会有 未分配 的空间，选择右键，新建简单卷，一步步操作下，就完成分区的了。